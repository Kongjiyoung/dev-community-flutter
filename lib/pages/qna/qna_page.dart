import 'package:flutter/cupertino.dart';

class QAndAPage extends StatelessWidget {
  const QAndAPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("QAndAPage"));
  }
}
