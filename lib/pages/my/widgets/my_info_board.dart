import 'package:flutter/material.dart';

class MyInfoBoard extends StatelessWidget {
  const MyInfoBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
